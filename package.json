{
  "name": "image-comment",
  "displayName": "Image Comment",
  "description": "%extension.description%",
  "version": "0.5.2",
  "publisher": "hekaigustav",
  "license": "MIT",
  "engines": {
    "vscode": "^1.74.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/HEKEH/vscode-image-comment.git"
  },
  "categories": [
    "Other"
  ],
  "keywords": [
    "image",
    "paste",
    "screenshot",
    "picture",
    "comment",
    "annotation",
    "code comment"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "icon": "images/icon.png",
  "contributes": {
    "configuration": {
      "title": "Image Comment",
      "properties": {
        "imageComment.saveDirectory": {
          "type": "string",
          "default": ".image-comment",
          "description": "%config.saveDirectory.description%"
        },
        "imageComment.commentTemplate": {
          "type": "string",
          "default": "![image-comment]({path})",
          "description": "%config.commentTemplate.description%"
        },
        "imageComment.useRelativePath": {
          "type": "boolean",
          "default": true,
          "description": "%config.useRelativePath.description%"
        },
        "imageComment.enableDecorations": {
          "type": "boolean",
          "default": true,
          "description": "%config.enableDecorations.description%"
        },
        "imageComment.showGutterIcon": {
          "type": "boolean",
          "default": true,
          "description": "%config.showGutterIcon.description%"
        },
        "imageComment.highlightBackground": {
          "type": "boolean",
          "default": true,
          "description": "%config.highlightBackground.description%"
        }
      }
    },
    "commands": [
      {
        "command": "imageComment.pasteImage",
        "title": "%command.pasteImage.title%",
        "icon": "$(file-media)"
      },
      {
        "command": "imageComment.previewImage",
        "title": "%command.previewImage.title%",
        "icon": "$(eye)"
      },
      {
        "command": "imageComment.deleteImageComment",
        "title": "%command.deleteImageComment.title%",
        "icon": "$(trash)"
      }
    ],
    "menus": {
      "editor/context": [
        {
          "command": "imageComment.pasteImage",
          "when": "editorTextFocus && !editorReadonly && !editorHasSelection",
          "group": "imageComment"
        }
      ]
    }
  },
  "scripts": {
    "preinstall": "npx -y only-allow npm",
    "publish": "ts-node ./scripts/vsce.ts publish",
    "vscode:prepublish": "npm run compile",
    "package": "ts-node ./scripts/vsce.ts package",
    "compile": "tsc -p ./ && npm run copy-nls",
    "postcompile": "ts-node scripts/check-after-compile.ts",
    "copy-nls": "cp src/nls*.json out/ 2>/dev/null || true",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.74.0",
    "dotenv": "^16.4.5",
    "sharp": "^0.34.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.0.0"
  }
}
